# 로컬 개발 인프라 — ws 게이트웨이는 Redis만 필요. PostgreSQL은 core-api 착수 시 추가.
services:
  redis:
    image: redis:7-alpine
    container_name: alphatalk-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

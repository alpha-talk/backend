프로젝트 지침의 단일 진실은 AGENTS.md다. 지침 수정은 AGENTS.md에서만 한다 — 이 파일에는 Claude Code 하네스에서만 동작하는 도구 설정만 덧붙인다.

@AGENTS.md

## 작업 흐름 (Claude Code 전용)

**브랜치 → 구현 → 자체 리뷰 → 교차 리뷰(Codex) → 통과 후 커밋·push.** 1·3단계는 사용자의 추가 지시 없이 에이전트가 스스로 수행한다.

### 1. 코드를 건드리기 전에 브랜치를 판다

- **작업 하나 = 브랜치 하나.** 작업을 시작하면 코드를 수정하기 전에 `main`에서 브랜치를 분기한다. `main` 워킹트리에 변경을 쌓지 않는다.
- 분기 전에 **`git fetch origin`을 먼저 돌리고** `git status -sb`로 현재 위치가 `main`이고 `origin/main`과 동기인지 확인한다. `git status -sb`는 로컬에 캐시된 `origin/main`과 비교할 뿐이라, fetch 없이는 원격이 앞서 있어도 "동기"로 보이고 낡은 코드에서 분기하게 된다. 체크아웃된 것이 머지 전 브랜치면 작업이 섞인다.
- **워킹트리가 깨끗한지도 함께 확인한다**(`git status --short --untracked-files=all`). 사용자의 기존 변경이 남아 있으면 브랜치를 파지 말고 먼저 사용자에게 알린다 — `git checkout -b`는 그 변경을 새 브랜치로 그대로 끌고 가고, 3단계의 자동 커밋이 남의 작업을 내 커밋에 섞어 넣는다.
- 이름은 `md/git_convention.md` §2 형식(`type/scope/desc`). 목적이 다른 변경을 한 브랜치에 섞지 않는다.
- 단, **직전 작업의 후속 변경**(같은 PR에 들어가야 하고 같은 주제·같은 파일을 손대는 것)은 새로 파지 않고 기존 브랜치에 쌓는다. `main`에서 새로 파면 같은 영역을 두 브랜치가 고쳐 머지 충돌이 나고, 앞 브랜치를 base로 삼는 스택 PR은 `md/git_convention.md` §5.1에서 금지된다.

### 2. 자체 리뷰 → 교차 리뷰

빌드·테스트 통과는 완료 조건이 아니라 리뷰 진입 조건이다.

1. **자체 리뷰**: 구현한 에이전트가 자기 변경을 적대적으로 다시 읽는다. `md/coding_convention.md` §5(운영 실패·규모)와 AGENTS.md의 불변 규칙 위반을 우선 확인하고, 발견이 없어도 "발견 없음"을 보고한다.
2. **교차 리뷰**: Codex가 같은 변경을 독립적으로 리뷰한다. 자체 리뷰와 교차 리뷰는 서로를 대체하지 않는다 — 같은 모델이 만든 사각지대는 같은 모델이 못 찾는다.
3. **지적 처리**: 리뷰 지적은 고치거나, 고치지 않을 근거를 남긴다. 조용히 넘기지 않는다.

**커밋 전 인-턴 교차 리뷰는 변경 단위가 아니라 작업 단위로 묶어 호출한다.** 한 작업에 파일·단계가 여럿이어도 변경마다 Codex를 부르지 말고, **계획한 변경을 전부 끝내고 빌드·테스트가 통과한 뒤 `review --wait`를 한 번** 실행한다 — 리뷰는 워킹트리 전체를 매번 새로 읽으므로 쪼개 부르면 아직 안 끝난 중간 상태에 대한 지적이 반복되고 시간·비용만 늘어난다. 지적을 고친 뒤의 재리뷰도 마찬가지로 **수정을 전부 반영한 뒤 한 번** 돌린다.

이 배치 원칙은 에이전트가 직접 실행하는 **인-턴 리뷰 호출에만** 적용한다. 아래 stop-time review gate는 리뷰 누락을 잡는 자동 backstop이라 코드 수정 턴이 끝날 때 별도로 실행될 수 있으며, 작업 단위 인-턴 리뷰를 대신하거나 커밋을 촉발하지 않는다.

### 3. 리뷰를 통과하면 커밋하고 push한다

- Codex 교차 리뷰가 **통과하면 별도 지시 없이 커밋한다.** 지적이 있으면 해소하고 다시 리뷰한다 — 통과 전에는 커밋하지 않는다.
- 통과 기준은 "지적 0"이 아니라 **"미해결 지적 0"**이다. 2단계에서 오탐·의도한 트레이드오프로 판단해 고치지 않기로 한 지적은 **기각 근거를 사용자에게 보고하고 커밋을 진행한다**. 인-턴 리뷰는 그 근거를 넘겨받지 못해 같은 지적을 계속 반복하므로, 지적 0을 기다리면 무한 루프에 빠진다. 재리뷰는 **고친 것이 있을 때만** 돌린다.
- 리뷰는 **커밋 전에 인-턴으로** 돌린다(에이전트는 companion 스크립트를 `review --wait`로 직접 실행). stop 게이트의 `ALLOW`는 턴을 끝내는 신호라 그 뒤에 커밋할 여지가 없다 — 게이트는 인-턴 리뷰를 빠뜨린 경우를 잡는 backstop이지 커밋 트리거가 아니다.
- **리뷰를 돌리기 전에** 워킹트리에 내 것이 아닌 변경이 있는지 확인한다. `review`는 스테이징 여부와 무관하게 **워킹트리 전체**를 대상으로 삼으므로, 브랜치를 판 뒤 사용자가 편집한 내용까지 리뷰에 들어간다. 그대로 두면 남의 작업에 대한 지적을 내가 고치려 들게 된다 — 커밋 시점의 헝크 확인은 이 단계를 대신하지 못한다. 섞인 변경을 발견하면 리뷰를 돌리지 말고 사용자에게 먼저 알린다.
- **이번 작업에서 직접 건드린 경로만 스테이징한다.** `git add -A`·`git commit -a`로 워킹트리를 통째로 쓸어담지 않는다 — 브랜치를 판 뒤 사용자가 따로 만든 변경이 섞인다.
- 경로 단위 스테이징만으로는 부족하다. 같은 파일을 사용자가 나중에 편집하면 `git status --short`에는 파일 하나가 수정됨으로만 보이고 양쪽 헝크가 함께 스테이징된다. 커밋 직전 **`git diff --staged`로 스테이징된 헝크가 전부 내가 만든 것인지 확인**하고, 출처가 불분명한 헝크가 있으면 커밋을 멈추고 사용자에게 알린다.
- 커밋 메시지는 `md/git_convention.md` §3 형식. 공동 저자 트레일러 금지.
- 커밋한 뒤 **작업 브랜치를 그대로 push한다**(첫 push는 `git push -u origin <branch>`). `--force`·`--force-with-lease`는 사용자가 요청할 때만 쓴다.
- **자동 범위는 push까지다. PR 생성·수정은 사용자가 명시적으로 요청할 때만 한다.**

## 코드 리뷰 게이트 (Codex)

2단계의 교차 리뷰는 Codex 플러그인의 **stop-time review gate**로 자동화한다. 켜두면 코드를 수정한 턴이 끝날 때마다 Codex 리뷰가 자동 실행되고, `BLOCK`이면 지적을 해소해야 턴이 종료된다. 코드 변경이 없는 턴은 그냥 통과한다.

```bash
/codex:setup --enable-review-gate    # 게이트 켜기
/codex:setup --disable-review-gate   # 끄기
/codex:review --wait                 # 커밋 전 인-턴 리뷰 (3단계에서 쓰는 형태)
/codex:review --background           # 큰 diff를 백그라운드로 리뷰
/codex:adversarial-review            # 커스텀 지시를 얹은 공격적 리뷰
```

에이전트가 인-턴 리뷰를 돌릴 때는 슬래시 명령 대신 companion 스크립트를 직접 실행한다 — 경로는 `~/.claude/plugins/cache/openai-codex/codex/<version>/scripts/codex-companion.mjs`이며 버전 디렉터리는 고정하지 말고 그때그때 찾는다.

리뷰에 쓰는 모델은 플러그인이 지정하지 않는다. `~/.codex/config.toml`의 `model`·`model_reasoning_effort`가 그대로 쓰이므로, 리뷰 모델을 바꾸려면 그 파일을 고친다(Codex CLI 전체에 함께 적용된다).

게이트 설정은 저장소가 아니라 `~/.claude/plugins/data/codex-inline/state/`에 워크스페이스 경로 기준으로 저장된다 — **작업 환경마다 각자 한 번 켜야 한다**. Codex CLI가 없거나 로그인이 안 되어 있으면 게이트가 동작하지 않으므로 `/codex:setup`으로 먼저 확인한다. 게이트가 꺼져 있어도 3단계의 인-턴 리뷰는 그대로 수행하며, **교차 리뷰 없이 코드 변경을 커밋하거나 완료로 보고하지 않는다**.
